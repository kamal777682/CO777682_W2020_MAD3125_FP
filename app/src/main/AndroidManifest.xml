<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kamal.co777682_w2020_mad3125_fp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.BillSummaryActivity"></activity>
        <activity android:name=".UI.AddNewCustomerActivity" android:parentActivityName=".UI.CustomerListActivity" />
        <activity
            android:name=".UI.ShowBillDetailsActivity"  android:parentActivityName=".UI.CustomerListActivity"/>
        <activity android:name=".UI.AddMobileBillActivity" />
        <activity android:name=".UI.AddInternetBillActivity" />
        <activity android:name=".UI.AddHydroBillActivity"/>
        <activity android:name=".UI.CustomerListActivity"  />
        <activity
            android:name=".UI.LoginActivity" />
        <activity android:name=".UI.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>